import { Flex } from "@sparrowengg/twigs-react";

const Dashboard = () => {
  return (
    <Flex
      css={{
        width: "1080px",
        height: "100%",
        backgroundColor: "$primary",
      }}
    ></Flex>
  );
};

export default Dashboard;
