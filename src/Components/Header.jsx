import { Flex } from "@sparrowengg/twigs-react";

const Header = () => {
  return (
    <Flex
      css={{
        height: "82px",
      }}
    >
      Header
    </Flex>
  );
};

export default Header;
